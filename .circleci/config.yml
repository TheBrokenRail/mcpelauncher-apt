version: 2
jobs:
  build:
    docker:
      - image: buildpack-deps:bionic
    steps:
      - run:
          name: Install Sudo
          command: apt install sudo
      - checkout
      - run:
          name: Build MCPELauncher
          command: ./build-mcpe.sh
      - run:
          name: Build Repo
          command: ./build-repo.sh
